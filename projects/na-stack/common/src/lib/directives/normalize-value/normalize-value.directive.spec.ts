import { NormalizeValueDirective } from './normalize-value.directive';

describe('NormalizeValueDirective', () => {
  it('should create an instance', () => {
    const directive = new NormalizeValueDirective();
    expect(directive).toBeTruthy();
  });
});

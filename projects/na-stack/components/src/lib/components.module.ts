import { NgModule } from '@angular/core';
import { COMPONENTS_COMPONENTS } from './components';

@NgModule({
  imports: [COMPONENTS_COMPONENTS],
  exports: [COMPONENTS_COMPONENTS],
})
export class ComponentsModule {}
